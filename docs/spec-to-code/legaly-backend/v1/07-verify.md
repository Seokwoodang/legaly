# 07 · 종합 검증 — 리걸리 백엔드

## 실행 결과 (2026-06-30)
- Vitest: **162 통과 / 0 실패** (프론트 128 회귀 + 백엔드 단위/계약 34). tsc 클린, vite build 성공.
- 라이브(배포 후 실연동):
  - `legal?kind=laws&team=법무` → 12건 JSON ✅
  - `answer`(로그인 토큰) → 200, 실제 Claude 답변 + RAG 출처 ✅ / (게스트) → **401** ✅
  - 인증+보관함: 로그인→insert→select(본인 1건)→다른 사용자 select 0건(RLS)→delete ✅
  - 마이그레이션 적용(테이블 2 + RLS), 계정 4개 시드 ✅

## 양방향 커버리지
- 전방(스펙→그리드): A/B/C/D/E 모든 결정이 그리드 셀로 존재.
- 후방(그리드→테스트/라이브): `05-traceability.md` 전 행 ✅. 미커버: 국가법령정보 실 상세(MST 매핑)·임베딩 — deferred.

## 백엔드 축 적합성(리뷰 r1으로 검증)
authn/authz(RLS·answer JWT), 입력검증, 멱등(시드/GET), 에러모델(7코드↔상태), 외부호출 실패(8s/20s
타임아웃→504/502, 캐시 폴백), rate limit(맵 정리), CORS(전 응답), 시크릿(서버 전용) — 모두 충족.
r1 7건 수정 + 라이브 재검증.

## 회귀
프론트 기존 128 테스트 GREEN(시그니처 불변·이중모드). 의도적 변경: 상담/authModal 테스트는 AI 로그인
게이트 반영(로그인 시드 추가). 그 외 무손상.

## 잔여 (deferred.md)
- 국가법령정보 실 상세(슬러그→MST 매핑) — 현재 스텁 폴백.
- 임베딩 벡터 검색(pgvector) — 키워드 RAG로 시작.
- 비번 `123456`(데모; 1234는 정책상 6자 미만 불가) → 운영 시 변경.
- 시크릿 로테이트 권장(채팅 노출).

## 결론
3 워크스트림(A/B/C) 구현·테스트·**라이브 배포·검증** 완료. Gate 2 준비 완료.
