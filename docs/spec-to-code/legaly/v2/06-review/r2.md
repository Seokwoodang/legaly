# 06 · v2 리뷰 라운드 r2 (r1 클로저)

r1 디스포지션(권장대로) 반영 확인. 모든 변경은 128 테스트 GREEN + 타입체크 + 빌드 + 시각으로 검증.

## r1 클로저 (비블라인드)
| r1 | 디스포지션 | 반영 |
|---|---|---|
| R1 딥링크 후 팀전환 분야 미리셋 | fix | ✅ prevTeamRef로 딥링크 1회 적용 + 팀 변경 시만 '전체' 리셋. 회귀 테스트 `guides.test` R1 추가(통과) |
| R2 로딩/없음 상태 AuthModal 누락 | fix | ✅ 두 early-return에 AuthModal 추가 |
| R3 dangerouslySetInnerHTML | fix | ✅ Icon을 JSX(ReactNode) 정적 렌더로 전환, guidesData ICON 상수 제거. 시각 확인(아이콘 정상) |
| R4 Icon size 死 prop | fix | ✅ size prop 제거, 컨테이너 100% 채움 |
| R5 교차팀 id accent 코스메틱 | keep | 디자인 충실(findGuide id 우선 무스코프) 유지 |
| R6 가이드 참조 판례 샘플 누락 | defer | `deferred.md` 기록(실제 코퍼스 연동 시 해소) |

## 검증
- Vitest 128 통과(추가 R1 회귀 포함), `tsc --noEmit` 클린, `vite build` 성공.
- Preview: 새 로비·가이드 목록/상세 × 팀 — JSX 아이콘·서브내비·틴트 정상.

## 종합
열린 블로커/메이저 없음. R5(디자인 충실)·R6(샘플 갭, deferred) 외 전부 수정. 라운드 승인 시 reviewApproved.
