# 06 · v2 리뷰 라운드 r1 (델타 diff)

독립 리뷰어가 v2 변경분(가이드/로비/아이콘/서브내비)을 v2 스펙 대비 검토.

### 🟡 R1 (메이저) — 가이드 목록: `?field=` 딥링크 후 팀 전환 시 분야가 '전체'로 리셋 안 됨
- `GuidesPage.tsx:23` — `useEffect([team])`가 `params.get('field')`를 다시 읽어 팀 전환해도 형사 유지 →
  새 팀에 그 분야 없으면 빈 목록. 그리드 J "팀 전환 → field 전체 리셋" 위반. 테스트 미커버.
- 수정: 딥링크는 마운트 1회만 적용, 팀 전환은 항상 '전체' 리셋(ref 분리). + 회귀 테스트 추가.
- **권장: fix**

### 🟡 R2 (메이저) — 가이드 상세 로딩/없음 상태에 AuthModal 누락
- `GuideDetailPage.tsx` 두 early-return에 Header(onOpenAuth)는 있으나 `<AuthModal>` 미렌더 → 그 상태에서
  로그인 버튼 무동작.
- 수정: 두 early-return에 AuthModal 추가.
- **권장: fix**

### 🟡 R3 (메이저, 스펙 일치) — Icon이 `dangerouslySetInnerHTML` 사용(내 resolved-spec §3 "미사용" 위반)
- `Icon.tsx:11`. 상수만 들어가 위험은 낮지만, **내가 쓴 확정 스펙이 명시적으로 금지**함. 일관성을 위해
  ICON을 JSX(ReactNode)로 바꿔 제거.
- **권장: fix (스펙 준수)**

### ⚪ R4 (마이너) — Icon `size` prop 死(style width/height:100%가 우선)
- 모든 호출부가 고정 span으로 감싸 100% 채움 → size 무효. 혼란 방지로 제거.
- **권장: fix (제거)**

### ⚪ R5 (마이너) — getGuide id 매칭이 팀 무스코프 → 교차팀 직접 URL 시 accent 불일치(코스메틱)
- 디자인의 `findGuide`도 동일(id 우선 무스코프). **디자인 충실**. 팀 전환 시 navigate('/guides')라 화면에
  남기 어려움.
- **권장: keep(디자인 충실)** — 원하면 팀 스코프로 변경 가능.

### ⚪ R6 (마이너) — 가이드 case 참조 `2018도2614`가 CASES 샘플에 없음 → 링크가 폴백(대표 판례) 표시
- criminal-complaint 가이드의 참고 판례. 샘플 데이터 갭(스펙상 샘플은 플레이스홀더). 실제 코퍼스 연동 시 해소.
- **권장: defer(deferred.md)** — 또는 해당 판례 샘플 추가.

## 종합
메이저 3(R1·R2·R3) 수정 필요. R4 정리, R5 디자인 충실 유지, R6 샘플 갭 보류. 양호: 필터/검색·로비
재작성·prevTeamRef·CTA pendingQ·FIELDIC 완비·서브내비·makeSlug 매핑 모두 정확.
