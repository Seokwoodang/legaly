# 06 · 리뷰 라운드 r3 — 리걸리 프론트엔드 (집중 회귀)

독립 리뷰어(블라인드, 이전 라운드 미공유) — r2 수정본 회귀 집중 검토.

## (b) r2 클로저 체크 — 비블라인드 (이전 라운드 수정 반영 확인)
| r2 | 수정 | 반영 확인 |
|---|---|---|
| N1 로비 미리보기 stale | LobbyPage dep `[location.key]` | ✅ r3 "per-visit 정확, clean" |
| N2 보관함 필터 미리셋 | SavedPage effect `setFilter('전체')` | ✅ r3 "spec §G 정확히 일치" |
| N3 이어서 2회차 드롭 | pendingQ effect 로컬 가드 + sentPendingRef 제거 | ✅ r3 "재발사 정상, dangling ref 없음" + `saved.test` N3 회귀 |
| N4 상세 언마운트 가드 | LawDetail/CaseDetail `alive` 플래그 | ✅ r3 "중첩 async까지 alive 공유 정확" |
→ r2 전 항목 정상 반영(회귀 없음).

## (a) 블라인드 신규 지적
### 🔴 R1 (방어적 강화) — pendingQ 발사 가드를 effect-로컬 변수 → 발사 시점 재-peek로
- 위치: `src/pages/ConsultPage.tsx` pendingQ effect
- 지적: 기존 `let fired` 로컬 가드는 StrictMode 이중 effect 간 공유되지 않아, 실질 보호는
  cleanup의 `clearTimeout`에만 의존(동기 정리라 현실적으론 안전하나 타이밍 의존적).
- 수정: 타이머 콜백에서 `peekPendingQ()` 재확인 + 동기 `clearPendingQ()`로 1회만 소비
  (어떤 타이머가 먼저 터지든 결정적). 죽은 `fired` 변수 제거.
- **처리: ✅ fix 적용** (라운드 내 반영). 검증: 106 테스트 GREEN, 라이브(StrictMode)에서 자동전송
  1회·pendingQ 소비 확인.

## 리뷰어 확인 양호(이번 라운드)
location.key per-visit 의미·SavedPage 필터 리셋·LobbyPage 재읽기·상세 alive 가드(중첩 포함)·
prevTeamRef 첫 마운트 무리다이렉트·이어서 2회차 재발사·doSave id(randomUUID)·"전체 보기" 항상 렌더 —
전부 clean. 보안 이슈 없음.

## 종합
신규 지적 1건(R1, 방어적 강화)만이며 라운드 내 수정·검증 완료. **열린 블로커/메이저 없음.**
→ 라운드 승인 시 `reviewApproved=true`.
